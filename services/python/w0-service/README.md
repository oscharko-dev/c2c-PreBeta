# Python W0 service

A minimal FastAPI-free Python service scaffold (stdlib-first) used as a deterministic W0 fixture.

Build and run commands:

```bash
cd services/python/w0-service
python -m unittest
```
