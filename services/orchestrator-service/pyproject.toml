[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "orchestrator-service"
version = "0.1.0"
description = "W0 orchestrator service (stdlib-only)."
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
