{
  "name": "c2c-ui",
  "version": "0.1.0",
  "private": true,
  "description": "W0 expert-facing UI for the c2c walking skeleton. Vanilla TypeScript + DOM, served by c2c-bff.",
  "scripts": {
    "build": "tsc && node scripts/copy-public.mjs",
    "lint": "tsc --noEmit",
    "test": "tsc --project tsconfig.test.json && node --test dist-test/view-model.test.js dist-test/api.test.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^20.14.11",
    "typescript": "^5.4.5"
  }
}
